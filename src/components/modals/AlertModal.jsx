import React from "react";
import { Background, Content } from "./styles";

const AlertModal = ({onClose, mode}) => {
  return (
      <Background>
        <Content>
          
         </Content>
      </Background>
  );
};

export default AlertModal;